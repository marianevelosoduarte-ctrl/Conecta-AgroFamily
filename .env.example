# Banco de dados PostgreSQL (ex: Railway, Neon, local)
DATABASE_URL="postgresql://usuario:senha@host:porta/banco"

# Segredo do NextAuth — gere com: openssl rand -base64 32
AUTH_SECRET="troque-por-um-segredo-aleatorio"
